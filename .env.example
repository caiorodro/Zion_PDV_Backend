# Copie este arquivo para ".env" (nunca commitado) e preencha com os valores reais.
# Ver sql/criar_usuario_app.sql para criar um usuário de banco com privilégio mínimo,
# em vez de usar a credencial root.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=zion
DB_USER=zion_app
DB_PASSWORD=
